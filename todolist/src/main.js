// import devtools from '@vue/devtools'
import { createApp } from 'vue'
import App from './App.vue'
import "./assets/main.css"

// if (process.env.NODE_ENV === 'development') {
//     devtools.connect('https://localhost:8080')
//   }

createApp(App).mount('#app')
